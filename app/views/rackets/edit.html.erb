<h1>Edit your Racket</h1>

<%= render partial: "layouts/errors", locals: {object: @racket} %>

<% if @racket.image.attached? %>
  <%= image_tag url_for(@racket.image) %>
<% end %>
<br>

<%= render partial: 'form'%>